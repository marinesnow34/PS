#include <iostream>
#include <string>

using namespace std;

int main(){
  char c_money[1002], c_per[1002], share[1002];
  scanf("%s %s", c_money, c_per);
  string money(c_money), per(c_per);
  
  while(money.length() > per.length()){
    if(money[0] > per[0]){
      
    }
    else{
      
    }
  }

  printf("%s", share);


  return 0;
}